# Render deployment

## Frontend Static Site

- Root directory: `frontend`
- Build command: `npm install && npm run build`
- Publish directory: `dist`

The repository includes `render.yaml` with the same settings. After pushing changes, use **Manual Deploy → Clear build cache & deploy** in Render.
